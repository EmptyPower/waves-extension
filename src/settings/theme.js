import { createMuiTheme } from 'material-ui/styles';

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#ff9a22',
			contrastText: '#fff',
		},
		secondary: {
			main: '#f1edff'
		},
	},
});

export default theme;